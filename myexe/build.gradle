model {
    components {
        myexe(NativeExecutableSpec) {
            sources {
                c {
                    lib project: ':mylib', library: 'mylib'
                }
            }
        }
    }
}
